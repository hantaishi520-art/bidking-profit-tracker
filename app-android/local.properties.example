# 签名配置模板 —— 复制本文件为 local.properties 并填入你自己的签名信息。
#
# local.properties 已在 .gitignore 中，不会进 Git；
# 只有你要打**正式签名的 release 包**时才需要它。
# 没有它也能构建（会退回默认签名，可以装到手机上调试，但不能覆盖安装正式版）。
#
# 生成自己的 keystore（只需做一次，丢了就永远无法给旧用户升级）：
#   keytool -genkey -v -keystore bidking-release.keystore -alias bidking -keyalg RSA -keysize 2048 -validity 10000
# keystore 文件和密码都要自己另外备份，绝不能提交到仓库。

KEYSTORE_PASSWORD=改成你的keystore密码
KEY_ALIAS=改成你的key别名
KEY_PASSWORD=改成你的key密码
