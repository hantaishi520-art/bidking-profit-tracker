# 安全须知（给第一次用这工具的朋友）

> 这份小抄就两件事：**怎么用是安全的**、**出了什么事不用慌**。不需要任何技术基础。

---

## 一、记住三句话

1. **家里 Wi-Fi 下手机连电脑，很简单** —— 填对访问密码（如果设了）就能连，不需要配对、不需要点头同意。
2. **只有走 ngrok 公网访问时才要「配对」** —— 公网上的陌生设备必须你在电脑上点「同意」才能连上。
3. **你的战绩（生涯数据库）永远不丢** —— 就算换电脑、重置密码、文件夹被拷走，战绩都在。

---

## 二、第一次连接手机（家里 Wi-Fi，两步搞定）

| 步骤 | 在哪做 | 做什么 |
|---|---|---|
| 1 | 电脑网页 | 页面顶部找到「另一台电脑访问」的地址（形如 `http://192.168.x.x:8766`） |
| 2 | 手机 App | 填这个地址 + 访问密码（没设密码就留空），点连接 → 直接能用 |

> **什么时候才需要「配对」？** 只有通过 ngrok 公网地址（不在家里、用流量访问家里的电脑）连接时，才需要到电脑网页「🔐 安全与配对」里点「同意」。同一 Wi-Fi 下完全不用。

---

## 三、四个安全开关（电脑网页「安全与配对」里，只管 ngrok 公网）

| 开关 | 默认 | 建议 |
|---|---|---|
| 公网新设备必须配对 | 开 | **保持开**，这是最要紧的一道门 |
| 允许公网旧版 App 只读 | 开 | 保持开即可（旧手机能看不能改） |
| 允许公网手机远程启动估价器 | **关** | 需要时自己打开，用完记得关回去（家里 Wi-Fi 的手机不受此限制） |
| 公网访问需密码+配对 | 开 | 保持开，不要为了省事关掉 |

---

## 四、换电脑 / 文件夹被拷走了，怎么办

- 程序会**自动检测到**换机/拷贝，**自动重置密码和配对**，然后网页顶部出现黄色提醒。
- **你的战绩数据完全不受影响**（生涯数据库、报表都在）。
- 你只需要：重新设置一个访问密码 → 重新配对手机 → 继续用。

---

## 五、发给朋友前，先跑一次「分享清理」

在 exe 所在文件夹打开命令行，运行：

```
BidKing解析器.exe --prepare-share
```

程序会：
- 删除你电脑上的密码、配对、ngrok token 等所有私人凭据；
- 打印「**该发什么**」和「**不该发什么**」两张清单。

> **只发这三个**：`BidKing解析器.exe`、`BidKing远程助手.apk`、`使用说明.md`
> **千万别发**：`生涯数据库/`（你的战绩）、`.bidking_sec.bin / .ngrok_token* / .bidking_pass`（你的凭据）、`item_prices.db / result*.json`

---

## 六、常见疑问

- **Q：我不设密码行不行？** 可以，但同 Wi-Fi 下别人可能连上你的电脑。建议设一个。
- **Q：密码忘了怎么办？** 在电脑上跑 `BidKing解析器.exe --reset-security`，密码会重置，重新设一个即可（战绩不丢）。
- **Q：手机重装 App / 删除重新添加服务器，要重新配对吗？** 不用。同一台设备在电脑端同意过一次后，凭访问密码就会自动重新连上（App v1.2 + 解析器 v1.2.0 起）；只有电脑端点了「撤销」的设备才需要重新配对。
- **Q：手机上被撤销了配对？** 说明电脑端有人点了「撤销」，重新配对一次就好。
- **Q：手机连上后能改我电脑的东西吗？** 不能。已配对的手机只能看报表和发启动请求；改密码、清数据库、改配置只能在电脑本机做。